<figure class="feedback-loop" aria-label="反馈循环：人沉淀 harness，Guides 引导 Agent 行动，Sensors 反馈并帮助 Agent 自我纠正，重复问题再沉淀回 harness">
  <div class="harness node">
    <span>Human</span>
    <strong>迭代 harness</strong>
  </div>

  <div class="forward arrow" aria-hidden="true"><span>沉淀引导</span></div>

  <div class="steering-loop">
    <div class="loop-title">
      <span>Steering Loop</span>
      <strong>驾驭闭环</strong>
    </div>
    <div class="loop-flow">
      <div class="node">
        <span>Guides · 前馈</span>
        <strong>行动前尽量做对</strong>
      </div>
      <div class="arrow" aria-hidden="true"></div>
      <div class="node agent">
        <span>Agent</span>
        <strong>行动</strong>
      </div>
      <div class="arrow" aria-hidden="true"></div>
      <div class="node">
        <span>Sensors · 反馈</span>
        <strong>行动后看见偏差</strong>
      </div>
    </div>
    <div class="correction" aria-hidden="true">
      <i></i>
      <span>信号回灌 · 自我纠正</span>
    </div>
  </div>

  <div class="return" aria-hidden="true">
    <span>重复出现的问题</span>
    <i></i>
  </div>
</figure>

<style>
  .feedback-loop {
    position: relative;
    display: grid;
    margin: 18px 0 4px;
    padding: 28px;
    grid-template-columns: minmax(126px, 0.62fr) 72px minmax(0, 2.6fr);
    align-items: center;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.11);
    border-radius: 12px;
    background:
      radial-gradient(circle at 82% 42%, rgba(245, 185, 113, 0.08), transparent 34%),
      rgba(0, 0, 0, 0.28);
  }

  .node {
    position: relative;
    display: flex;
    min-height: 82px;
    padding: 15px;
    flex-direction: column;
    justify-content: center;
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.035);
  }

  .node span,
  .loop-title span,
  .return span,
  .correction span,
  .forward span {
    color: var(--accent);
    font: 10px/1.4 var(--mono);
    letter-spacing: 0.06em;
  }

  .node strong,
  .loop-title strong {
    margin-top: 4px;
    color: var(--ink);
    font-size: 13px;
    font-weight: 560;
  }

  .harness {
    border-color: rgba(245, 185, 113, 0.32);
  }

  .arrow {
    position: relative;
    height: 1px;
    background: rgba(245, 185, 113, 0.42);
  }

  .arrow::after {
    position: absolute;
    top: -3px;
    right: -1px;
    width: 7px;
    height: 7px;
    border-top: 1px solid var(--accent);
    border-right: 1px solid var(--accent);
    content: "";
    transform: rotate(45deg);
  }

  .forward span {
    position: absolute;
    right: 8px;
    bottom: 7px;
    white-space: nowrap;
  }

  .steering-loop {
    position: relative;
    padding: 18px 18px 46px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.018);
  }

  .loop-title {
    display: flex;
    margin-bottom: 14px;
    align-items: baseline;
    gap: 8px;
  }

  .loop-title strong {
    margin: 0;
  }

  .loop-flow {
    display: grid;
    grid-template-columns: minmax(126px, 1fr) 34px minmax(90px, 0.7fr) 34px minmax(126px, 1fr);
    align-items: center;
  }

  .agent {
    text-align: center;
  }

  .correction {
    position: absolute;
    right: 18px;
    bottom: 12px;
    left: 48%;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .correction i {
    position: relative;
    height: 14px;
    flex: 1;
    border-bottom: 1px dashed rgba(245, 185, 113, 0.38);
    border-left: 1px dashed rgba(245, 185, 113, 0.38);
    border-radius: 0 0 0 7px;
  }

  .correction i::before {
    position: absolute;
    bottom: -4px;
    left: -1px;
    width: 7px;
    height: 7px;
    border-bottom: 1px solid var(--accent);
    border-left: 1px solid var(--accent);
    content: "";
    transform: rotate(45deg);
  }

  .return {
    position: absolute;
    right: 28px;
    bottom: 7px;
    left: 28px;
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .return i {
    height: 10px;
    flex: 1;
    border-bottom: 1px dashed rgba(255, 255, 255, 0.2);
    border-left: 1px dashed rgba(255, 255, 255, 0.2);
    border-radius: 0 0 0 6px;
  }

  @media (max-width: 720px) {
    .feedback-loop {
      padding: 20px 18px 28px;
      grid-template-columns: 1fr;
      gap: 18px;
    }

    .forward {
      width: 1px;
      height: 30px;
      justify-self: center;
    }

    .forward::after {
      top: auto;
      right: -3px;
      bottom: 0;
      transform: rotate(135deg);
    }

    .forward span {
      right: auto;
      bottom: 8px;
      left: 10px;
    }

    .steering-loop {
      padding: 18px;
    }

    .loop-flow {
      grid-template-columns: 1fr;
      gap: 12px;
    }

    .loop-flow .arrow {
      width: 1px;
      height: 22px;
      justify-self: center;
    }

    .loop-flow .arrow::after {
      top: auto;
      right: -3px;
      bottom: 0;
      transform: rotate(135deg);
    }

    .correction {
      position: relative;
      right: auto;
      bottom: auto;
      left: auto;
      margin-top: 14px;
    }

    .return {
      display: none;
    }
  }
</style>
